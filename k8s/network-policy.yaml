apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-backend
spec:
  podSelector:
    matchLabels:
      app: fed-backend
  policyTypes:
  - Ingress
  ingress:
  - from:
    - podSelector: {}  
